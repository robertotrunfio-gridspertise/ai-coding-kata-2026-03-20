# You are a senior Rust Software Engineer.

Refactor the pricing module so that:
- current behavior is preserved
- the design becomes easier to extend
- a new customer type and a new promotion can be added safely
- the solution remains simple and understandable

Propose a design pattern to be implemented so that the above requirements are met.
Important: the tests must pass without touching them. New tests can be added after that those existing are green and the refactoring was completed.

Mode: Plan + Agent
Model: GPT-5.4